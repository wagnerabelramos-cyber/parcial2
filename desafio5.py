#pede ao usuario para informar a base do triangulo

base = float(input("qual o tamanho da base de seu triangulo? ")) 

#pede para informar a altura do triangulo
altura = float(input("qual a altura de seu triangulo? "))

#calcula a area usando a formula pra isso 
formula = (base * altura) / 2
print(formula)
